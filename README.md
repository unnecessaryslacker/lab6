# Лабораторна робота №6
## Тема: Колекції в Java

### Виконав:
**Савченко Андрій**, група **ІО-35**  
Номер залікової книжки: **3517**

---

## 🎯 Мета роботи
Ознайомитися з принципами створення власних колекцій у Java, використання інтерфейсів колекцій (`Set`, `List` тощо), а також реалізацією власної структури даних. Закріпити навички роботи з ітераторами, внутрішніми класами та узагальненнями (generics).

---
## 🔢 Варіант
Розрахунок виконується згідно з методичкою:
C2 = 3517 % 2 = 1
C3 = 3517 % 3 = 2


✔ **C2 = 1 → Колекція повинна реалізовувати інтерфейс Set**  
✔ **C3 = 2 → Внутрішня структура – двозв’язний список**

---

## 🧩 Завдання варіанту
Створити власну типізовану колекцію, яка:

- реалізує інтерфейс **Set**
- внутрішньо побудована на **двозв’язному списку**
- працює з типом **RailCar** (базовий клас з ЛР5)
- містить мінімум **3 конструктори**:
    - конструктор без параметрів
    - конструктор з одним елементом
    - конструктор, що приймає іншу колекцію
- реалізує основні методи:
    - `add(T value)`
    - `remove(Object o)`
    - `contains(Object o)`
    - `iterator()`
    - `size()`
    - `clear()`
    - `isEmpty()`

---

## 🛠 Структура проєкту
- **RailCar.java** – базовий абстрактний клас (взятий з ЛР5)
- **MyLinkedSet.java** – власна колекція на базі двозв’язного списку
- **Main.java** – демонстрація роботи колекції
- **README.md** – опис лабораторної

---

## 📌 Короткий опис реалізації

### 🔸 RailCar
Абстрактний клас з ЛР5, що містить базові поля:
- `id`
- `comfort`
- абстрактні методи `getPassengers()` та `getLuggage()`

Використовується як тип елементів у власній колекції.

---

### 🔸 MyLinkedSet
Власна колекція, яка:

- реалізує **Iterable<T>** (можливість foreach)
- запобігає дублюванню елементів (вимога до Set)
- зберігає дані у **двозв’язному списку**
- містить внутрішній статичний клас `Node`
- реалізує всі основні методи Set

---

### 🔸 Main
Показує роботу колекції:

- додавання елементів
- перевірка розміру
- ітерація через foreach
- перевірка `contains`
- видалення елемента

